<nav class="navbar navbar-expand-md navbar-dark bg-dark">
  <div class="container">
    <a class="navbar-brand" href="{{ path_for('web.index') }}">E/Code</a>
    <button
      class="navbar-toggler d-lg-none"
      type="button"
      data-toggle="collapse"
      data-target="#collapsemenu"
      aria-controls="collapsemenu"
      aria-expanded="false"
      aria-label="Abrir menu"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    
    <div class="collapse navbar-collapse justify-content-center" id="collapsemenu">
      <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
        <li class="nav-item{{ is_current_router('web.index') ? ' active' : '' }}">
          <a class="nav-link" href="{{ path_for('web.index') }}">Listar Pessoas</a>
        </li>
        <li class="nav-item{{ has_current_router('/pessoas') ? ' active' : '' }}">
          <a class="nav-link" href="{{ path_for('web.person') }}">Cadastrar Pessoas</a>
        </li>
      </ul>
    </div>
    
    <div class="d-flex align-items-center">
      <p class="mb-0 p-0 mr-4 text-white">{{ auth.name|raw }}</p>
      <a
        href="{{ path_for('web.logoff') }}"
        class="btn btn-danger btn-sm my-2 my-sm-0"
      >Sair
      </a>
    </div>
  </div>
</nav>
